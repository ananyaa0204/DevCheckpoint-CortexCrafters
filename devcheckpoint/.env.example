# Used only by the Prisma CLI (migrate/generate) for local development.
# The running app resolves its real database path at runtime via
# src/lib/db/app-data.ts (OS-specific app-data directory) and ignores this
# value in production.
DATABASE_URL="file:./dev.db"
